# Create secret: kubectl apply -f secret.yaml
# Or via CLI: kubectl create secret generic db-secret \
#   --from-literal=username=postgres \
#   --from-literal=password=yourpassword

apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
stringData:
  username: postgres
  password: yourStrongPassword123
